--6 수업자료--
1. 변수 선언 방법은 다 해줘야 됨
2. 자료형에서 보면 템플릿 문자열이랑 symbol이 있는데 이건 다 해줘야됨.
3. 함수 
>> 화살표 함수만 알면 되는데 어찌됐든 이벤트 핸들러를 쓰려면 함수 표현식도 알아야 됨
>>화살표 함수는 다 해야됨
>> this 바인딩 되는 건 알고있어야함 (화살표함수랑 일반 함수냐 객체안에 잇는거에 따라 달라지기때문에)
4. 매개변수 정의 방법 개선(매개변수 기본 값, 나머지 매개변수, 즉시 실행함수 제외)
5. 중첩함수 알아야 됨
6. 콜백함수 알아야 됨
7. 클로저 제외
8. 객체 >> 객체 리터럴은 반드시 알아야 함
9. 클래스 반드시 알아야 함 
10. 프로퍼티 존재 확인 안나옴
11. Module 안나옴.
12. String은 수업시간에 다뤘던 함수, 실습시간에 과제할때 다뤘던 함수들 그정도까지만 하면 됨.
>> includes, repeat, startsWith, endsWith(6버전에서 추가된 메소드)
13. 배열 Array
>> 유사 배열 객체, 이터러블 객체는 자세히 할 필요 없음.
>> 원소 추가 & 삭제 정도는 알고 있어야 함
14. Array 고차함수는 전부 다 해야함.
15. spread 문법 , destructing 문법 다 해줘야 함

--7 수업자료--
1. DOM 
>> 요소 노드 선택하는 방법 알아야 됨
>> 노드 탐색과 관련이 된 것은 안해도됨
2. 텍스트 조작 다 알아야 함
3. attribute 조작, style조작은 항상 하던 작업들이였으니까...?
4. 클래스 조작
>>className 프로퍼티에 대해서만 하기
>>classList 프로퍼티는 안해도 됨
5. DOM 조작
>> 새로운 노드 생성, 뒷 부분에 추가하는 거, 삭제하는 작업, 앞에 추가하는 작업,
6. 이벤트
>> 이벤트 핸들러 작성하는 3가지 방법만 해주면 됨.
7. 이벤트 디폴트 행동, 흐름, 웹 브라우저 객체 모델 이후에도 쭉 안해도 됨.

--8 수업자료--

1. jquery 개념은 거의 다 포함이 된다로 보면 됨.
2. jquery 선택자는 지금까지 계속 해오던 거랑 다를게 없음.
>> 더 공부할 건 없음(?)
3. 필터 
>> 배열관리 전은 다 건너뛰고, 배열관리는 해줘야 됨.
6. 문서 객체 선택과 탐색
>> 검색한 노드 다루기
>> end(), slice(),  안해도됨
7. 문서 객체 조작
>> 문서 객체 조작 관련 메소드는 해줄 필요가 있음
>> empty(), clone() 제외
8. 문서 객체 조작 >> 요소 크기 다루기는 할 필요 없음
9. 문서 객체 삽입
>> (메소드) append, prepend, after, before 만 알면 됨
10. 


--------이건 뭔소린지 모르겠음---------
이벤트에 대해서는 수업시간에 설명
두가지 방법으로 할 수 있음
실습 과제에서도 다뤄지는 게 있음
어니(?)라는 메소드를 사용해서
클릭을 하는 이벤트가 발생하면서 어ㅓ떻게 처리하는지
기본적으로 클릭이다라는 이벤트가 발생하면 어떻게 처리하는지 
기본적인 이벤트 처리방법 정도는 알아야함.